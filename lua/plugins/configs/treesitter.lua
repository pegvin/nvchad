local options = {
  ensure_installed = { "lua", "vim", "c", "cmake", "css", "make", "html" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
